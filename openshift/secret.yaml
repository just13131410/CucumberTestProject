# DEPRECATED – replaced by HashiCorp Vault (see openshift/vault/)
#
# Use this file ONLY as a fallback when Vault is not available (e.g. plain Minikube or local dev).
# When Vault is active, remove the secretRef block in deployment.yaml and do NOT apply this file.
#
# To create the secret manually instead of applying this file:
#   oc create secret generic cucumber-test-secrets \
#     --from-literal=ZEPHYR_API_TOKEN=your-token \
#     --from-literal=JIRA_API_TOKEN=your-token \
#     -n test-automation
apiVersion: v1
kind: Secret
metadata:
  name: cucumber-test-secrets
  namespace: test-automation
  labels:
    app: cucumber-test-service
type: Opaque
stringData:
  # Replace with actual values before applying
  ZEPHYR_API_TOKEN: "REPLACE_ME"
  JIRA_API_TOKEN: "REPLACE_ME"
