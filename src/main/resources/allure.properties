# Allure results directory is set dynamically per test run via TestContext.
# This default is only used when running tests outside the service (e.g. mvn test directly).
allure.results.directory=target/allure-results
